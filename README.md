# Stem-Robotic-Tracker

## Monorepo structure

- `apps/web` — Next.js STEMTrack platform
- `apps/mobile` — Expo VOICE-A1 mobile capture app
- `packages` — shared packages for future use
- `docs` — architecture and project documentation

## Run web
```bash
cd apps/web
npm install
npm run dev